# Setting CDPATH is a way to more fluently move in your directory hierarchy with
# <TAB> completion. The values in here do not necessarily reflect your directory
# structure. Feel free to change to your liking.

# The example here caused `cd` to look for <TAB> completion in the order
# they are specified. First the current directory first(I recommend to keep the current
# directory first or the behaviour may confuse yoy), the 

export CDPATH=.:~/work/orc:~/work/platform:~
